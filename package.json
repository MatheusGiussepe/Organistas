{
  "name": "organistas",
  "version": "1.0.0",
  "private": true,
  "description": "Lista de Organistas - sobe backend + frontend juntos com um comando.",
  "scripts": {
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently --names \"API,WEB\" --prefix-colors \"yellow,cyan\" \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "import": "npm --prefix backend run import"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
